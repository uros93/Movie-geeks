<% @users.each do |u| %>
    <% if u != current_user %>
       <p class: "text-left" >
       <%= link_to display_avatar(u), user_path(u) %><%= u.name %>  <%= u.email %>
        <%= render :partial => 'follows/form', :locals => {:user => u} %>
        </p> 
    <% end %>
<% end %>
