<% @users.each do |u| %>
    <% if u != current_user %>
       <p><%= u.name %>  <%= u.email %>
        <%= render :partial => 'follows/form', :locals => {:user => u} %>
        </p> 
    <% end %>
<% end %>
